import styled from "styled-components"

export const Nav = styled.div`
    ul {
        display: flex;
    }
    li {
        margin-left: 1rem;
    }
    a {
        color: #fff;
        padding: 0.5rem;
        &:hover {
            color: #eee;
        }
    }
    font-size: 1.6rem;
`
